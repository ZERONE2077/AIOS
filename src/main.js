import { createApp } from 'vue';
import App from './App.vue';
import './assets/styles/global.css'; // 引入全局样式
import './assets/styles/layout.css'; // 引入全局样式



createApp(App).mount('#app'); // 确保只有这个挂载
